[project]
name = "run"
version = "0.3.0"
description = "Automation script for Solution simulations"
authors = []
dependencies = [
    "cosmotech-api @ git+https://github.com/Cosmo-Tech/cosmotech-api-python-client.git",
    "python-dotenv>=1.0.0",
    "pyyaml>=6.0.1",
    "rich>=13.7.0",
    "python-keycloak>=0.26.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.metadata]
allow-direct-references = true
